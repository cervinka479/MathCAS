fixed all the previous problems of my classification models... (dataset issue >>> bin-datasets)

created classTest model using without scaling (scaling breaks binary outputs) > good results (accuracy 99.7%)
< modelArchitecture = nnArch(io=[3,1], hl=[32,16]) >

